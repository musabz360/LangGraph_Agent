# fetch_lead.py
# ---------------------------
# Dummy implementation for retrieving CRM leads.
# Replace with real database/API calls when available.

def fetch_lead() -> list:
    """
    Returns a list of leads (dummy data).
    """
    # Each dict simulates a lead record with minimal fields
    return [
        {"id": 1, "name": "Alice Smith", "email": "alice@example.com"},
        {"id": 2, "name": "Bob Johnson", "email": "bob@example.com"},
    ]
